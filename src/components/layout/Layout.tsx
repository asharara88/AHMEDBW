import React from 'react';

// This file should contain layout components, not page components
// The FitnessPage component has been moved to src/pages/fitness/FitnessPage.tsx
// where it belongs and is properly wrapped with AuthProvider

export default function Layout() {
  return (
    <div>
      {/* Layout components should go here */}
    </div>
  );
}